@model List<CategoryViewModel>

<div class="sidebar-box">
    <div class="categories">
        <h3>Kategoriler</h3>
        @foreach (var item in Model)
        {
            <li><a href="#">@item.CategoryName <span>(@item.CarCount)</span></a></li>
        }        
    </div>
</div>